# Copie os valores abaixo pra secao "Environment Variables" do projeto na
# Vercel (Settings > Environment Variables, ambiente "Production"). Esse
# arquivo eh so documentacao - a Vercel nao le arquivos .env do repo.

# URL publica do backend no Render, sem barra no final.
VITE_API_BASE_URL=https://lagoagro-backend.onrender.com
